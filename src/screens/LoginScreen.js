import React from "react";
import LoginButton from "../componets/buttons/LoginButton";

export const LoginScreen = () => {
    return(
        <div>
            <div style={{padding: '10px'}}>hola, esta es la prueba de la perra pantalla de login</div>
            <LoginButton/>
        </div>
    );
};